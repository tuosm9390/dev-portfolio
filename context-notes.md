- 2026-05-15: The requested redesign is centered on the first viewport, not the whole content model. Existing project data and lower sections should stay intact unless the new interaction requires otherwise.
- 2026-05-15: The selected-state layout should feel like macOS Stage Manager: project list collapses to a left vertical stack, and the selected project occupies the main workspace.
- 2026-05-15: Three.js should serve the folder/file-stack depth and motion. Text, links, and readable project detail should remain HTML for accessibility and SEO.
- 2026-05-15: The worktree is already dirty with user changes across many files. Avoid reverting or reformatting unrelated edits.
- 2026-05-15: Implemented the new first viewport as `PortfolioStage`, mounted through `HeroSection` so existing page composition, tests, project data, About, Contact, and Footer can remain in place.
- 2026-05-15: Used vanilla `three` instead of React Three Fiber to keep the dependency surface small. The canvas renders animated textured planes from existing project images, while the clickable Stage Manager UI stays in HTML.
- 2026-05-15: Verification notes: targeted ESLint passed for the changed components, production build passed, Vitest passed. Full `npm run lint` still fails because `.agents/skills/gstack` files are included by the repo lint command and contain pre-existing lint errors.
- 2026-05-15: User clarified that the existing frontend view and style should be treated as absent. The home page is now rebuilt as a single Stage Manager style 3D portfolio screen instead of layering the new hero on top of the previous section-based portfolio.
- 2026-05-15: Removed unused legacy home components after the rebuild: Header, HeroSection, section components, contact form, project modal, and the now-unused UI dialog/button helpers.
- 2026-05-15: User requested a frontend-view reset excluding project data. Treat `src/data/projects.ts` and project image assets as preserved data, while reducing app views/styles/components to a minimal baseline.
- 2026-05-15: Removed `PortfolioStage`, its CSS module, `JsonLd`, and the `three` / `@types/three` dependencies. Home and project detail routes now render blank `main` baselines while keeping project route generation and metadata from existing data.
- 2026-05-15: Verification after reset passed with targeted Vitest, targeted ESLint, and `npm run build`. Build still reports the expected minimal-metadata `metadataBase` warning after layout reset.
- 2026-05-15: New implementation request: ignore existing `DESIGN.md`, use a white background, and make Three.js the core technology. Performance and memory optimization are first-order constraints, especially texture size, render-loop activity, and disposal on unmount.
- 2026-05-15: User rejected the landing-page-like top copy, side margins, and framed box. Correct direction is a full-browser Three.js workspace where the folder box itself owns the first screen.
- 2026-05-15: Reworked the home screen so the browser viewport is entirely owned by the Three.js canvas. Stage Manager behavior now relies on Three.js file objects moving into the left quarter; the visible HTML rail was removed and only keyboard-accessible hidden buttons remain.
- 2026-05-15: Playwright visual verification passed after installing browser binaries outside the sandbox: desktop 1440x900 and mobile 390x844 both reported full-screen canvas/main metrics and nonblank PNG pixel counts. Mobile camera was widened after the first screenshot showed the folder was over-cropped.
- 2026-05-15: User clarified that project files must not be flat cards on the floor. They should read as upright files/books inserted in a shelf, arranged in a single row and viewed from around a 45 degree floor-based angle.
- 2026-05-15: Replaced floor-card geometry with upright book/file-spine boxes in a single shelf row. Removed project image textures from the 3D list and use lightweight generated spine labels instead; selected project images still load only in the detail panel.
- 2026-05-15: Shelf layout visual verification passed with Playwright PNG checks at 1440x900 and 390x844. Final screenshots: `D:\tmp\folder-portfolio-desktop-shelf-final.png` and `D:\tmp\folder-portfolio-mobile-shelf-final.png`.
- 2026-05-15: User requested only orientation changes from the current shelf state: camera position 90 degrees to the right and shelf objects 90 degrees to the right relative to that camera direction.
- 2026-05-15: Rotated camera from the previous +Z view to the +X side and wrapped the shelf/file objects in a Y-axis 90 degree `stageGroup`. Since camera and objects rotate together, visual composition stays intentionally similar while the scene orientation changes.
- 2026-05-21: User requested another frontend reset. Preserve portfolio project data and assets, but remove the current Three.js-driven view and return the home UI to a minimal React/Next baseline.
- 2026-05-21: Reset home to a minimal `chan.works` shell, deleted the Three.js folder portfolio files, and removed `three` / `@types/three` from dependencies. `npx vitest run src/app/__tests__/page.test.tsx`, targeted ESLint, and `npm run build` passed. Full `npm run lint` still fails on pre-existing `.agents/skills/gstack` lint issues outside the touched app code.
- 2026-05-21: Implementation request is now based on the confirmed prerequisites: Apple-inspired white layout, Framer Motion section reveal, same-screen project selection, desktop CSS 3D book opening, and simpler mobile fallback. Preserve existing user changes in data/assets/scripts.
- 2026-05-21: Implemented the Framer Motion portfolio home with `src/components/home/*`. Verification passed for `npx vitest run src/app/__tests__/page.test.tsx`, targeted ESLint on changed app/components, `npm run build`, and local `http://localhost:3001` HTTP content check. Full `npm run lint` still fails on existing `.agents/skills/gstack` lint errors outside this change.
- 2026-05-21: User requested Synapso.dev project description refresh from `D:\development\auto-blog\DESCRIPTION.md`. The long `description` already mostly matches Project Memory, but the card-facing fields still describe the earlier blog automation SaaS and should be aligned to Project Memory.
- 2026-05-21: Updated Synapso.dev summary/origin/brandSignal/productFlow/keyDecisions/proofSignals/techStack to match Project Memory. Verification passed with focused Vitest, focused ESLint, and `npm run build`. Did not commit because `src/data/projects.ts` already had substantial pre-existing uncommitted user changes in the same file.
- 2026-05-21: User requested the same description refresh for Minions Bid from `D:\development\league-auction\DESCRIPTION.md`. The long description already matches the source document, but card-facing optional fields should be filled/aligned from the product scope, realtime architecture, security model, and season-management pipeline.
- 2026-05-21: Updated Minions Bid summary/origin/brandSignal/productFlow/keyDecisions/proofSignals/techStack from the league-auction DESCRIPTION. Verification passed with focused Vitest, focused ESLint, and `npm run build`. Did not commit because `src/data/projects.ts` still contains substantial pre-existing uncommitted user changes in the same file.
- 2026-05-27: Scroll issue analysis found that `Page3DRoller` treats all wheel input through an 80px accumulator and also gives `.scroll-container` regions priority until their internal scroll reaches an edge. The fix should keep internal scroll behavior for real overflow sections, but make normal section transitions respond to a short wheel gesture.
- 2026-05-27: Implemented scroll fix by adding a same-index/bounds guard, an immediate `isAnimatingRef` lock, a shared active-scroll-state helper, and a lower 32px wheel transition threshold at section boundaries. Removed the duplicate `about` id from `StrengthsSection`; section hashes now rely on the wrappers in `page.tsx`.
- 2026-05-27: New request targets the selected-project book transition and a description modal. Assumptions: the existing `ProjectBook` selected state remains the interaction surface, the full implementation text comes from `project.description`, and the modal should be local state rather than a route change.
- 2026-05-27: Slowed the desktop book opening from 0.85s to 1.6s and delayed the page reveal to 0.55s. Added a Radix modal opened by `구현내용`, rendering the representative image above markdown-parsed `project.description`. Verification passed with focused Vitest, targeted ESLint, and `npm run build`.
- 2026-05-27: Modal scroll bug assumption: the global `Page3DRoller` wheel/touch handlers are still observing events while the Radix project description modal is open. Fix should short-circuit global navigation when `[data-project-description-modal]` exists and avoid costly `backdrop-filter` blur on the overlay.
- 2026-05-27: Implemented modal scroll priority by making `Page3DRoller` ignore wheel/touch/keyboard navigation while the description modal exists, stopping wheel/touch propagation inside the modal, and assigning the markdown body a `flex-1 min-h-0 overflow-y-auto overscroll-contain` scroll area. Removed `backdrop-blur-sm` from the overlay. Focused Vitest, targeted ESLint, and `npm run build` passed. Browser e2e was attempted, but the repo lacks `@playwright/test`.
- 2026-05-27: New modal layout request is visual only. Keep the Radix modal behavior and markdown data unchanged, but move the representative image into the same scrollable content section as the title/body and reduce its vertical footprint.
- 2026-05-27: Moved the description modal image into the same scrollable white section as the title and markdown body, changed the image from a separate `16/9` header to a smaller rounded `16/7` image, and kept the close button anchored at the modal top right. Focused Vitest, targeted ESLint, and `npm run build` passed.
- 2026-06-02: New `geul` feature request targets portfolio storytelling. Primary audience is hiring managers, with collaborators and future self as secondary readers. The hidden author route should support writing short or long Markdown posts with topic metadata and a live preview, while public readers should only see published posts.
- 2026-06-02: User chose Firestore for persistence. Initial model should use a `geul_posts` collection with the post `slug` as document id, `draft`/`published` status, Firestore timestamps, and Firebase Auth email gating for author-only writes. Public `/geul/[slug]` reads should reject drafts and missing posts.
- 2026-06-02: Implemented `geul` with Firebase client SDK, hidden `/geul` editor, `/geul/[slug]` public reader, validation tests, editor tests, and reader tests. Verification passed with focused Vitest, targeted ESLint, `npm run build`, and gstack browse QA for `/geul` plus missing public post state. Local Firebase env vars are not configured, so live Firestore save was not exercised in this environment.
- 2026-06-02: User requested password-based verification instead of email verification. Secure direction is server-side `scrypt` hash comparison with `GEUL_PASSWORD_HASH`, signed HTTP-only `geul_session` cookie with `GEUL_SESSION_SECRET`, and Firebase Admin writes from protected API routes. Client-side hash comparison is avoided because it would expose the hash to the browser.
- 2026-06-02: Password auth implementation replaced Firebase Auth email gating. `/api/geul/session` verifies the password hash and sets an HTTP-only cookie, `/api/geul/posts` requires that cookie for list/save, and public `/api/geul/posts/[slug]` returns `{ post: null }` when Firestore/Admin config is missing or the post is absent so the reader can show a clean missing state. Verification passed with focused Vitest, full Vitest, targeted ESLint, `npm run build`, and browse QA.
- 2026-06-05: New planning request is for a portfolio-only conversational AI agent. Existing codebase is a Next.js portfolio with canonical content in `src/data/profile.ts`, `src/data/projects.ts`, and an AI-readable `src/app/llms.txt/route.ts`. No assistant UI/API exists yet, and no AI SDK dependency is installed. Because user did not answer preference questions, planning defaults are global floating widget, Gemini-first provider, question-language answers, request-time context assembly from existing data, strict refusal for unsupported topics, and no visitor data capture in v1.
- 2026-06-05: Created `plans/portfolio-ai-agent.md` after Metis gap analysis. The plan fixes scope and implementation decisions: shared `src/lib/portfolio-context.ts`, `@google/genai` with `gemini-2.5-flash-lite`, `GEMINI_API_KEY`, single-turn non-streaming `/api/assistant`, deterministic pre/post refusal gates, fixed Korean/English refusal text, no phone disclosure, no persistence, Vitest TDD, curl API QA, and browser QA for desktop/mobile/widget layering.
- 2026-06-05: Momus plan review first returned ITERATE because `tmux` QA steps are not executable in this Windows/PowerShell workspace. Replaced those scenarios with PowerShell and `Tee-Object` evidence capture. Momus retry returned OKAY and confirmed referenced files exist, tasks are startable, QA scenarios are executable, and no blocking contradictions remain.
- 2026-06-05: User changed the assistant UI direction from a floating widget to a header-centered `ask me!` menu item. Updated `plans/portfolio-ai-agent.md` so Task 4 now builds a top-center Dynamic Island style chat popup opened from `src/components/home/Header.tsx`, with mobile menu behavior, Framer Motion animation, no bottom-right launcher, and revised Browser QA evidence paths.
- 2026-06-05: User asked to review the plan in Korean. Replaced `plans/portfolio-ai-agent.md` with a Korean version while preserving code paths, commands, API schemas, test IDs, evidence paths, commit messages, and implementation constraints.
- 2026-06-05: Began implementation from the Korean plan. Added a shared portfolio context builder so `llms.txt` and the assistant use the same canonical profile/project data. The assistant prompt excludes phone data, refuses unrelated topics before provider calls, and stores no visitor conversation.
- 2026-06-05: Installed `@google/genai` for the planned Gemini provider. Live supported-question answers require `GEMINI_API_KEY`; unsupported questions still return deterministic refusal without using the provider.
- 2026-06-05: Header integration uses a centered desktop `ask me!` trigger between ABOUT and PROJECTS. Mobile `ask me!` closes the menu before opening the same controlled top-center Dynamic Island style assistant popup.
- 2026-06-05: Focused Vitest passed for portfolio context, assistant policy/provider handling, `/api/assistant`, `PortfolioAssistantIsland`, and `Header` integration.
- 2026-06-05: Browser QA found that rendering the assistant dialog inside a desktop-only nav container made the mobile dialog hidden after the mobile menu closed. Fixed by separating header triggers from the shared assistant dialog layer rendered outside responsive containers.
- 2026-06-05: Final verification passed with full Vitest, targeted ESLint for changed files, `npm run build`, API QA for invalid/unsupported/supported-without-key cases, and browse QA screenshots for desktop/mobile assistant island. Full `npm run lint` still fails on pre-existing `.agents`, scripts, generated files, and unrelated app warnings.
